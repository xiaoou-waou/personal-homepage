# 验证记录

2026-09-11，本地预览地址 http://127.0.0.1:4178 。

## 入场欢迎语

按 AI 产品经理定位，将「欢迎你，进入我的视听世界」改为「欢迎你，一起把想法变成产品」，说明改为「我是小欧，关注 AI、Agent 与人机协作。」；英文提示为 `FROM IDEAS TO PRODUCTS`。同步更新按钮的无障碍名称和声音等待提示，避免旧文案残留。只改文字，入场时长、动效、音乐和静音进入逻辑不变。文案集中在 `PROFILE.entry`，构建检查覆盖生成结果及运行时提示。

## 文章入口与四张翻卡

原“持续探索”卡片是方向介绍，按钮绑定联系弹窗；顶部“文章”则跳至更下方的文章列表。已将四张卡片改为 `CONTENT.articles` 的前四篇真实文章，标题和“阅读全文”使用对应原文链接，保留原卡片结构、配色与翻卡引擎。

顶部导航、菜单精选文章、项目区文章入口统一定位到 `#writing`。卡片下提供“查看全部 5 篇文章”，原 `#recognition` 的完整列表继续保留。新增两条回归检查先在旧输出上失败，再在修复后通过；另用实际 `mountDialogs` / `mountServiceCards` 做内存 DOM 事件测试，确认文章链接不被联系弹窗拦截、键盘与减弱动态行为保留。

浏览器验收：点击顶部“文章”后 URL 为 `#writing`；1440px 下四张卡片标题均无横向溢出、卡片高度保持 445px。实际点击第一篇“阅读全文”后新标签页打开对应微信公众号原文 URL，联系窗口保持关闭。375px 下文档宽度为 375px、首卡宽 330px，无横向滚动。截图为 `previews/articles-desktop.png` 与 `previews/articles-mobile.png`。

## 首页项目与页脚调整

「公众号一键排版」（xiaoou-gzh-layout）已替换 Snaploom 成为精选区第一张大卡，封面标题、介绍、类型与链接同步更新。Snaploom 保留在下方其他项目列表；11 个项目均保留。截图中的 `.site-credit` 段落已从构建结果移除，而非仅用 CSS 隐藏。重新构建不会恢复该段说明。`npm run check` 新增了这两项需求的回归检查。

按后续“页脚说明全部删除”要求，继续移除了页脚章节小字、英文引导、联系说明段落、版权行及固定在页面底部的无脚本提示。保留联系标题与操作入口、公开账号、品牌标识和流体动效；构建时直接删除说明节点，避免再次生成。

## 后续照片调整

按用户补充要求，仅取消个人照片容器的黑白滤镜；默认、悬停、键盘聚焦和触屏状态都保持原色。模糊对焦的 WebGL 模块、鼠标跟随及对焦框没有修改。用户随后提供了照片路径，现已原样复制为 `assets/personal/portrait.jpeg`（896×1195），个人介绍与联系卡均改为该图。下载目录的源文件、旧头像和原网站存档均保留。

## 原档保护

`npm run check`：12 项通过、0 失败。

四个原文件（父目录 `index.html`、`个人网站/index.html`、`.gitignore`、`.nojekyll`）SHA256 均与 `archive-baseline.json` 一致。Git 只增加本次新文件夹；原先未跟踪的 `个人网站/` 仍保持原状态。没有提交、推送或部署。

## 复刻依据

- 原首页 9 个 CSS 按原顺序合并。逆转资源路径本地化并去掉首行说明后，与原样式逐字节一致，SHA256 为 `bb712638d18bd22e3ffb3de2c3a673619e6fddc3a1666359b9c80c60c39b8224`。
- 首屏、图像对焦与页脚直接调用原站独立引擎；音频与 GlowField 从原函数精确提取，只做输入/路径适配。
- 原 React 页面入口、RSC 数据与遥测脚本不加载，个人资料不会被原站 hydration 覆盖。
- 额外个人样式只用于长文字、6 字母标识的手机适配、个人联系卡及无障碍终态。

## 实际浏览器检查

- 桌面入场页、三维首屏、关于、菜单与联系弹窗已打开并截图检查。首屏实际状态 `data-scene="ready"`，并非只检查代码存在。截图见 `previews/desktop.png`。
- 静音入场后 2.8 秒关闭遮罩；菜单打开、锚点跳转和 Escape 关闭联系弹窗通过。
- 长按控件的键盘 Space 测试：`data-holding="true"`，显示 `0.08×`；松键释放。
- 音频按钮可进入 `playing` 状态，再次点击关闭；测试结束保持关闭。
- 3 个 GlowField 均进入 ready，页脚流体进入 `data-ready="true"`。
- 320、375、414、768 像素宽度检查：文档宽度不超过视口；减少动态模式下卡片正面可访问、长按控件禁用、布局不依赖运动。
- 375 像素手机端的作品、关于与联系区域已实际检查。个人标识比原站更长，已单独调整手机字号以防裁切。
- 7 处图片节点全部加载成功，图片自然宽度非零。已修复参考 SSR `srcSet` 大小写导致旧响应式候选残留的问题。
- WebGL 创建失败时静态原版 fallback 可正常展示；测试中一次上下文创建失败后重新加载恢复 ready。不承诺低端设备帧率，未测移动真机 60fps。

## 静态检查说明

运行了 finesse-ui 检测器。编号、章节英文小标题和原配色来自用户指定参考站，按复刻要求保留。`1.00×` 是真实时间倍率，不是虚构业绩。检测器逐文件扫描时认为 `personal.css` 的动效标记缺少引擎，是跨文件检查盲点：引擎实际位于本地 JS 模块，已由浏览器 ready 状态和渲染截图验证。

## 明确差异

- 页面使用小欧的真实身份、文章和项目，所以文字长度、章节总高度与参考站不同。
- 三张项目封面来自本人仓库 GitHub OG；公众号排版和 image-recon 为标注的文字封面。未借用原作者电影画面冒充项目截图。
- 原奖项区改为文章与思考；原二维码联系区改为已提供的公开账号。没有虚构奖项、私人联系方式或收费服务。
- 因以上内容差异，这是保留同款结构与动效的个人化复刻，不是整页逐像素相同，也没有执行自动像素差分验收。
